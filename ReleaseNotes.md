# Changes
- It's now possible to add epubs that don't have a cover image.

# Fixes
- The OPDS catalog now will correctly reflect the last update time of the books.
- Fixed some issues that could cause the import folder to crash/freeze the UI when adding a large number of books (tested with 10k books).
- The layout of the smart collection creation/editing page has been adjusted and optimized to handle larger lists of authors, series, etc.